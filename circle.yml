machine:
    services:
        - docker

dependencies:
    pre:
        - make container

test:
    override:
        - make test
        - docker run -it $(awk {'print $1'} .docker_image)  php --version | grep "^PHP 7"
